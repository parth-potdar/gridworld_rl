# GRIDWORLD-RL
Experimenting with RL from scratch on 2D gridworlds.

Install: `pip install -e .`

(Editable install just makes a pointer to the source directory, instead of building real wheel and installing to /site-packages/ -> changes are directly reflected without needing to reinstall package)    